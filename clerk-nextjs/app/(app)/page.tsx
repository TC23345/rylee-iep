// Overview — placeholder for Phase 1 shell verification.
// Phase 3 replaces this with the rendered README.md via the Markdown component.
export default function OverviewPage() {
  return (
    <div className="md-body mx-auto max-w-[72ch]">
      <h1>Overview</h1>
      <p>
        Shell scaffold is live. This page renders <code>README.md</code> once the
        markdown + content layer lands (Phase 3).
      </p>
    </div>
  );
}
